import { useLocale } from '../i18n/context';
import type { UiData } from '../data/ui';

// Small SSR landing. The game, mission configuration and canvas stay behind the explicit import boundary.
export default function CareerLauncher({ enter, data }: { enter?: () => void; data: UiData }) {
  const { locale, href, t } = useLocale();
  return <div class="arcade-launcher"><p class="eyebrow">ANTONIOS.EXE / JOB ROUTE</p><h1>JOB ROUTE »</h1><p class="lead">{locale === 'es' ? 'Tu carrera es la campaña.' : 'Your career is the campaign.'}</p><p>{locale === 'es' ? 'Nueve etapas con escenario propio: del caos de un piso compartido a grandes oficinas y al trabajo en remoto. Completa cuatro misiones por empresa, resuelve incidencias, encuentra los teléfonos gigantes y juega a Snake en el 3310. Después te espera un desafío final de siete fases. Escenarios y retos recreados con humor; trayectoria profesional real.' : 'Nine chapters with distinct settings: from shared-flat chaos to large offices and remote work. Complete four missions per company, resolve incidents, find giant phones and play Snake on the 3310. Then take on a seven-phase final challenge. Settings and challenges recreated with humor; a real professional career.'}</p><button class="button primary no-print" onClick={enter}>{t('ENTER')} ↗</button><div class="actions"><a class="button" href={href('/cv/')}>{locale === 'es' ? 'Ver CV completo' : 'View full CV'} ↗</a><a href={href('/experience/')}>{locale === 'es' ? 'Explorar la trayectoria' : 'Explore the career'} ↗</a></div><p class="muted">{locale === 'es' ? 'Campaña de exploración o misión rápida directa · música activada por defecto · guardado local · contrarreloj opcional · español e inglés.' : 'Exploration campaign or direct quick mission · music enabled by default · local saves · optional time challenges · Spanish and English.'}</p><details><summary>{locale === 'es' ? 'La carrera real, sin necesidad de jugar' : 'The real career, no gameplay required'}</summary><ul>{data.professionalExperience.map(job => <li key={job.id}><a href={href(`/experience/#${job.id}`)}>{job.company}</a> · {job.period} · {job.role}</li>)}</ul></details></div>;
}
