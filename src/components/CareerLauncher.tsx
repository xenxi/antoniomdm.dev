import { useLocale } from '../i18n/context';
import type { UiData } from '../data/ui';

// Small SSR landing. The game, mission configuration and canvas stay behind the explicit import boundary.
export default function CareerLauncher({ enter, data }: { enter?: () => void; data: UiData }) {
  const { locale, href, t } = useLocale();
  return <div class="arcade-launcher"><p class="eyebrow">ANTONIOS.EXE / JOB ROUTE</p><h1>JOB ROUTE »</h1><p class="lead">{locale === 'es' ? 'Tu carrera es la campaña.' : 'Your career is the campaign.'}</p><p>{locale === 'es' ? 'Un RPG de bolsillo a color: explora una ciudad con parques, luces de neón y oficinas isométricas. Entra por sus puertas y completa cada etapa para desbloquear la siguiente. El mundo es ficción; la carrera que contiene es real.' : 'A colorful pocket RPG: explore a town with parks, neon lights and isometric offices. Enter their doors and complete each chapter to unlock the next. The world is fiction; the career within it is real.'}</p><button class="button primary no-print" onClick={enter}>{t('ENTER')} ↗</button><div class="actions"><a class="button" href={href('/cv/')}>{locale === 'es' ? 'Ver CV completo' : 'View full CV'} ↗</a><a href={href('/experience/')}>{locale === 'es' ? 'Explorar la trayectoria' : 'Explore the career'} ↗</a></div><p class="muted">{locale === 'es' ? 'Teclado y controles táctiles · guardado local · empresas que se desbloquean siguiendo el CV.' : 'Keyboard and touch controls · local saves · companies unlock in CV order.'}</p><details><summary>{locale === 'es' ? 'La carrera real, sin necesidad de jugar' : 'The real career, no gameplay required'}</summary><ul>{data.professionalExperience.map(job => <li key={job.id}><a href={href(`/experience/#${job.id}`)}>{job.company}</a> · {job.period} · {job.role}</li>)}</ul></details></div>;
}
